<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Alophia.Views.StagesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <StackPanel Spacing="8">
            <TextBlock Text="Project Timeline" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock Text="Gantt chart with simulated project phases" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Gantt Chart Canvas -->
        <canvas:CanvasControl
            x:Name="GanttChart"
            Grid.Row="1"
            Background="{ThemeResource ControlFillColorDefaultBrush}"
            Draw="GanttChart_Draw"
            Margin="0,16,0,0"/>

        <!-- Legend -->
        <StackPanel Grid.Row="2" Spacing="8" Margin="0,24,0,0">
            <TextBlock Text="Legend" FontSize="14" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Spacing="24">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Rectangle Width="24" Height="8" Fill="#EEEDFE"/>
                    <TextBlock Text="Planned" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Rectangle Width="24" Height="8" Fill="#534AB7"/>
                    <TextBlock Text="Elapsed" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
